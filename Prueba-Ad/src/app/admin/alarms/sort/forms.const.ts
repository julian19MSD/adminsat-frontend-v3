import {FormControl} from '@angular/forms';

export const formFields= {
  evento_nombre: new FormControl(),
  activo_nombre: new FormControl(),
  placa: new FormControl(),
  reporte_fecha: new FormControl(),
};
