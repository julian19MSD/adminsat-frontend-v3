import {FormControl} from '@angular/forms';

export const formFieldsSort = {
  cliente_nombre: new FormControl(),
  is_active: new FormControl(),
  tipo_actor_nombre: new FormControl(),
  apellidos: new FormControl(),
  nombre: new FormControl()
};
