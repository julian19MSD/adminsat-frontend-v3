import {FormControl} from '@angular/forms';


export const formFields = {
    cliente__in: new FormControl([], []),
    nombre__in: new FormControl([], []),
    apellidos__in: new FormControl([], []),
    numero_documento_identidad__in: new FormControl([], [])
  };
  