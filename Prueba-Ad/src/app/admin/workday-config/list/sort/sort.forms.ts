import {FormControl} from '@angular/forms';

export const formFieldsSort = {
  cliente: new FormControl(),
  workday_duration: new FormControl(),
  workday_alert: new FormControl(),
  rest_duration: new FormControl(),
  rest_alert: new FormControl(),
  rest_limit_speed: new FormControl(),

};
